/**
 * Form model for vriend worden (friend) form
 * Used with Angular Signal Forms (experimental in Angular 21)
 */
export interface VriendWordenFormModel {
  name: string;
  email: string;
  subject: string;
  message: string;
}

