/**
 * Form model for sinterklaas form
 * Used with Angular Signal Forms (experimental in Angular 21)
 */
export interface SinterklaasFormModel {
  name: string;
  email: string;
  subject: string;
  message: string;
}

